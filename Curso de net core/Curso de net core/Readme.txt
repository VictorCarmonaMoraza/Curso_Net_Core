## 48 - Argumentos con nombres

//Con el signo d einterrogacion le podemos pasar valores nulos a cantidad, en los string no hace falta porque
aceptan valores nulo

data.metodo3(null, null);

private void metodo3(String curso, int? cantidad, string nombre="Jorge")
        {
            Console.WriteLine(cantidad);
        }